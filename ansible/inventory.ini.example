# Example inventory for Ansible deployment.
# For GitHub Actions, the following secrets can be used:
#   - SSH_HOST: Host to connect to (defaults to DOMAIN_NAME if not set)
#   - SSH_PORT: SSH port (defaults to 22 if not set)
#   - DOMAIN_NAME: Fallback host if SSH_HOST is not set

[solidarity_servers]
mrezasolidarnosti.org ansible_user=root ansible_port=22

[all:vars]
ansible_python_interpreter=/usr/bin/python3
