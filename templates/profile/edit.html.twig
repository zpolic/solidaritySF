{% extends 'base.html.twig' %}

{% block body %}
    <div class="card card-lg bg-base-100 shadow m-10">
        <div class="card-body">
            <h2 class="card-title mb-4">
                <span class="ti ti-user text-2xl"></span>
                Izmena korisničkih podataka
            </h2>

            {{ include('flashes.html.twig') }}

            <div>
                {% form_theme form 'form.html.twig' %}
                {{ form(form) }}
            </div>

            <div class="card-actions justify-end">
                <a href="{{ path('profile_change_password') }}" class="btn btn-ghost">
                    <span class="ti ti-lock text-2xl"></span>
                    Izmena lozinke
                </a>
            </div>
        </div>
    </div>
{% endblock %}
