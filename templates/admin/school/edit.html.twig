{% extends 'admin/base.html.twig' %}

{% block content %}
    <div class="text-xl">
        <span class="ti ti-building text-2xl"></span>
        Škola
    </div>

    <div class="divider"></div>

    {{ include('flashes.html.twig') }}

    {% form_theme form with ['form.html.twig'] %}
    {{ form(form) }}
{% endblock %}